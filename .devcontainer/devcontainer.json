{
  "name": "R Development",
  "image": "mcr.microsoft.com/devcontainers/base:debian",
  "features": {
    "ghcr.io/rocker-org/devcontainer-features/r-rig:1": {
      "version": "release",
      "installREnv": false,
      "installRMarkdown": false,
      "installJupyterlab": false
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libxml2-dev libfontconfig1-dev libfreetype6-dev libharfbuzz-dev libfribidi-dev libpng-dev libtiff5-dev libjpeg-dev libcurl4-openssl-dev libssl-dev cmake && R -e \"install.packages(c('jsonlite', 'languageserver', 'devtools', 'httpgd', 'lintr', 'httptest2), repos = 'https://cloud.r-project.org')\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "anthropic.claude-code"
      ],
      "settings": {
        "editor.rulers": [80]
      }
    }
  }
}
